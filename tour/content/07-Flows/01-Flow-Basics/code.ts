{
  "$schema": "https://calm.finos.org/draft/2025-03/meta/flow.json",
  // Add the basic flow properties:
  // - unique-id
  // - name
  // - description
  
  // Add a participants array with at least 3 node IDs
  
  // Add a steps array with at least 2 steps
  // Each step should have:
  // - unique-id
  // - name
  // - description
  // - from (a participant ID)
  // - to (a participant ID)
  // - sequence (a number)
}