{
  "$schema": "https://calm.finos.org/draft/2025-03/meta/control.json",
  // Add the basic properties for a control:
  // - unique-id
  // - name
  // - description
  // - control-type (set to "security")
  
  // Add a requirements array with at least one requirement
  // Each requirement should have:
  // - unique-id
  // - description
  // - severity (high, medium, or low)
}