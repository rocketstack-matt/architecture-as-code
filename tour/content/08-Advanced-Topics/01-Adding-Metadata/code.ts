{
  "$schema": "https://calm.finos.org/draft/2025-03/meta/calm.json",
  "nodes": [
    {
      "unique-id": "order-service",
      "node-type": "service",
      "name": "Order Service",
      "description": "Handles order processing"
      // Add metadata properties:
      // - data-classification (set to "Confidential")
      // - owner (set to a team name)
      // - version (set to a version number)
      // - status (set to "active")
      // - Add at least one custom metadata property of your choice
    }
  ]
}