{
  "$schema": "https://calm.finos.org/draft/2025-03/meta/calm.json",
  "nodes": [
    // Define a system node here
    // Set node-type to "system"
    
    // Define two service nodes that will be part of the system
    // Each should have a unique-id, node-type, name, and description
  ],
  "relationships": [
    // Add a composed-of relationship to show that the system contains the two services
    // Use the composed-of relationship type
  ]
}