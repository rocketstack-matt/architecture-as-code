/**
 * Re-export of the helpers from @finos/calm-design-tokens.
 * Kept as a local module for backwards compatibility with existing imports.
 */

export {
    getNodeTypeColor,
    getRiskLevelColor,
    getAdrStatusColor,
} from '@finos/calm-design-tokens'
